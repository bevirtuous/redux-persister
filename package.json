{
  "name": "@virtuous/redux-persister",
  "version": "1.0.0-beta.4",
  "description": "Store enhancer that syncs (a subset of) your redux store state to localStorage.",
  "license": "MIT",
  "main": "./dist",
  "scripts": {
    "build": "rm -rf dist/ && NODE_ENV=production babel src --out-dir dist --no-comments",
    "preversion": "yarn run build",
    "postversion": "git push --all && git push --tags && npm publish --access=public --tag=beta"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/virtuous/redux-persister.git"
  },
  "bugs": {
    "url": "https://github.com/virtuous/redux-persister/issues"
  },
  "homepage": "https://github.com/virtuous/redux-persister#readme",
  "keywords": [
    "redux",
    "localstorage",
    "persist"
  ],
  "contributors": [
    "Florian Müller <florian.mueller81@gmx.de> (https://github.com/devbucket)"
  ],
  "devDependencies": {
    "@virtuous/eslint-config": "^1.4.0",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-plugin-transform-es2015-modules-umd": "^6.24.1",
    "babel-plugin-transform-export-extensions": "^6.22.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.7.0",
    "babel-runtime": "^6.26.0"
  },
  "dependencies": {
    "@virtuous/logger": "1.0.0-beta.4",
    "lodash": "^4.17.10"
  }
}
